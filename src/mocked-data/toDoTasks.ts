import { TaskList } from "../entities/cvInterfaces"

const ToDoList: TaskList = {
    toDoTasks: [
        'edit button does not close the PersonalInfo edit mode',
        'Print + Download',

    ],
    completedTasks: [
        'Components in the right column stay open after edit button toggle',
        'Cancel Edit button'
    ]
}
export default ToDoList